<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	
	
	<!--
	 	lazyLoadingEnabled:全局性设置懒加载。如果设为‘false’，则所有相关联的都会被初始化加载。(默认值:false)
	 	aggressiveLazyLoading:当设置为‘true’的时候，懒加载的对象可能被任何懒属性全部加载。否则，每个属性都按需加载。(默认值:true)
	 -->
	<!--  <settings>  --> 
         <!-- 打开延迟加载的开关 changes from the defaults -->  
         <!-- <setting name="lazyLoadingEnabled" value="true" /> --> 
         <!-- 将积极加载改为消极加载及按需加载 -->
         <!-- <setting name="aggressiveLazyLoading" value="false"/> -->
    <!--  </settings>   -->


	
</configuration>